// This file was auto-generated by Fern from our API Definition.

import * as Management from "../../../api/index";
import { ManagementClient } from "../../../Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("ExperimentsClient", () => {
    test("advanceRamp (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 1 };
        const rawResponseBody = { experiment_id: "experiment_id", from_level: 1, to_level: 1, current_level: 1 };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.experimentation.experiments.advanceRamp("id", {
            target_level: 1,
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("advanceRamp (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 100 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.experimentation.experiments.advanceRamp("id", {
                target_level: 100,
            });
        }).rejects.toThrow(Management.BadRequestError);
    });

    test("advanceRamp (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 100 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.experimentation.experiments.advanceRamp("id", {
                target_level: 100,
            });
        }).rejects.toThrow(Management.UnauthorizedError);
    });

    test("advanceRamp (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 100 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.experimentation.experiments.advanceRamp("id", {
                target_level: 100,
            });
        }).rejects.toThrow(Management.ForbiddenError);
    });

    test("advanceRamp (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 100 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.experimentation.experiments.advanceRamp("id", {
                target_level: 100,
            });
        }).rejects.toThrow(Management.NotFoundError);
    });

    test("advanceRamp (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 100 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.experimentation.experiments.advanceRamp("id", {
                target_level: 100,
            });
        }).rejects.toThrow(Management.ConflictError);
    });

    test("advanceRamp (7)", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { target_level: 100 };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/experimentation/experiments/id/advance-ramp")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(429)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.experimentation.experiments.advanceRamp("id", {
                target_level: 100,
            });
        }).rejects.toThrow(Management.TooManyRequestsError);
    });
});
