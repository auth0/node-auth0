/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../mock-server/MockServerPool.js";
import { ManagementClient } from "../../Client.js";

describe("Organizations", () => {
    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { name: "name" };
        const rawResponseBody = {
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: { logo_url: "logo_url", colors: { primary: "primary", page_background: "page_background" } },
            metadata: { key: "value" },
            token_quota: { client_credentials: { enforce: true, per_day: 1, per_hour: 1 } },
            enabled_connections: [
                {
                    connection_id: "connection_id",
                    assign_membership_on_login: true,
                    show_as_button: true,
                    is_signup_enabled: true,
                },
            ],
        };
        server
            .mockEndpoint()
            .post("/organizations")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.create({
            name: "name",
        });
        expect(response).toEqual({
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: {
                logo_url: "logo_url",
                colors: {
                    primary: "primary",
                    page_background: "page_background",
                },
            },
            metadata: {
                key: "value",
            },
            token_quota: {
                client_credentials: {
                    enforce: true,
                    per_day: 1,
                    per_hour: 1,
                },
            },
            enabled_connections: [
                {
                    connection_id: "connection_id",
                    assign_membership_on_login: true,
                    show_as_button: true,
                    is_signup_enabled: true,
                },
            ],
        });
    });

    test("getByName", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: { logo_url: "logo_url", colors: { primary: "primary", page_background: "page_background" } },
            metadata: { key: "value" },
            token_quota: { client_credentials: { enforce: true, per_day: 1, per_hour: 1 } },
        };
        server
            .mockEndpoint()
            .get("/organizations/name/name")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.getByName("name");
        expect(response).toEqual({
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: {
                logo_url: "logo_url",
                colors: {
                    primary: "primary",
                    page_background: "page_background",
                },
            },
            metadata: {
                key: "value",
            },
            token_quota: {
                client_credentials: {
                    enforce: true,
                    per_day: 1,
                    per_hour: 1,
                },
            },
        });
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: { logo_url: "logo_url", colors: { primary: "primary", page_background: "page_background" } },
            metadata: { key: "value" },
            token_quota: { client_credentials: { enforce: true, per_day: 1, per_hour: 1 } },
        };
        server.mockEndpoint().get("/organizations/id").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.organizations.get("id");
        expect(response).toEqual({
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: {
                logo_url: "logo_url",
                colors: {
                    primary: "primary",
                    page_background: "page_background",
                },
            },
            metadata: {
                key: "value",
            },
            token_quota: {
                client_credentials: {
                    enforce: true,
                    per_day: 1,
                    per_hour: 1,
                },
            },
        });
    });

    test("delete", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/organizations/id").respondWith().statusCode(200).build();

        const response = await client.organizations.delete("id");
        expect(response).toEqual(undefined);
    });

    test("update", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: { logo_url: "logo_url", colors: { primary: "primary", page_background: "page_background" } },
            metadata: { key: "value" },
            token_quota: { client_credentials: { enforce: true, per_day: 1, per_hour: 1 } },
        };
        server
            .mockEndpoint()
            .patch("/organizations/id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.update("id");
        expect(response).toEqual({
            id: "id",
            name: "name",
            display_name: "display_name",
            branding: {
                logo_url: "logo_url",
                colors: {
                    primary: "primary",
                    page_background: "page_background",
                },
            },
            metadata: {
                key: "value",
            },
            token_quota: {
                client_credentials: {
                    enforce: true,
                    per_day: 1,
                    per_hour: 1,
                },
            },
        });
    });

    test("associateClientGrant", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { grant_id: "grant_id" };
        const rawResponseBody = {
            id: "id",
            client_id: "client_id",
            audience: "audience",
            scope: ["scope"],
            organization_usage: "deny",
            allow_any_organization: true,
        };
        server
            .mockEndpoint()
            .post("/organizations/id/client-grants")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.associateClientGrant("id", {
            grant_id: "grant_id",
        });
        expect(response).toEqual({
            id: "id",
            client_id: "client_id",
            audience: "audience",
            scope: ["scope"],
            organization_usage: "deny",
            allow_any_organization: true,
        });
    });

    test("deleteClientGrant", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/organizations/id/client-grants/grant_id").respondWith().statusCode(200).build();

        const response = await client.organizations.deleteClientGrant("id", "grant_id");
        expect(response).toEqual(undefined);
    });

    test("addConnection", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { connection_id: "connection_id" };
        const rawResponseBody = {
            connection_id: "connection_id",
            assign_membership_on_login: true,
            show_as_button: true,
            is_signup_enabled: true,
            connection: { name: "name", strategy: "strategy" },
        };
        server
            .mockEndpoint()
            .post("/organizations/id/enabled_connections")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.addConnection("id", {
            connection_id: "connection_id",
        });
        expect(response).toEqual({
            connection_id: "connection_id",
            assign_membership_on_login: true,
            show_as_button: true,
            is_signup_enabled: true,
            connection: {
                name: "name",
                strategy: "strategy",
            },
        });
    });

    test("getConnection", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            connection_id: "connection_id",
            assign_membership_on_login: true,
            show_as_button: true,
            is_signup_enabled: true,
            connection: { name: "name", strategy: "strategy" },
        };
        server
            .mockEndpoint()
            .get("/organizations/id/enabled_connections/connectionId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.getConnection("id", "connectionId");
        expect(response).toEqual({
            connection_id: "connection_id",
            assign_membership_on_login: true,
            show_as_button: true,
            is_signup_enabled: true,
            connection: {
                name: "name",
                strategy: "strategy",
            },
        });
    });

    test("deleteConnection", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/organizations/id/enabled_connections/connectionId")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.organizations.deleteConnection("id", "connectionId");
        expect(response).toEqual(undefined);
    });

    test("updateConnection", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {
            connection_id: "connection_id",
            assign_membership_on_login: true,
            show_as_button: true,
            is_signup_enabled: true,
            connection: { name: "name", strategy: "strategy" },
        };
        server
            .mockEndpoint()
            .patch("/organizations/id/enabled_connections/connectionId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.updateConnection("id", "connectionId");
        expect(response).toEqual({
            connection_id: "connection_id",
            assign_membership_on_login: true,
            show_as_button: true,
            is_signup_enabled: true,
            connection: {
                name: "name",
                strategy: "strategy",
            },
        });
    });

    test("createInvitation", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { inviter: { name: "name" }, invitee: { email: "email" }, client_id: "client_id" };
        const rawResponseBody = {
            id: "id",
            organization_id: "organization_id",
            inviter: { name: "name" },
            invitee: { email: "email" },
            invitation_url: "invitation_url",
            created_at: "2024-01-15T09:30:00Z",
            expires_at: "2024-01-15T09:30:00Z",
            client_id: "client_id",
            connection_id: "connection_id",
            app_metadata: { key: "value" },
            user_metadata: { key: "value" },
            roles: ["roles"],
            ticket_id: "ticket_id",
        };
        server
            .mockEndpoint()
            .post("/organizations/id/invitations")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.createInvitation("id", {
            inviter: {
                name: "name",
            },
            invitee: {
                email: "email",
            },
            client_id: "client_id",
        });
        expect(response).toEqual({
            id: "id",
            organization_id: "organization_id",
            inviter: {
                name: "name",
            },
            invitee: {
                email: "email",
            },
            invitation_url: "invitation_url",
            created_at: "2024-01-15T09:30:00Z",
            expires_at: "2024-01-15T09:30:00Z",
            client_id: "client_id",
            connection_id: "connection_id",
            app_metadata: {
                key: "value",
            },
            user_metadata: {
                key: "value",
            },
            roles: ["roles"],
            ticket_id: "ticket_id",
        });
    });

    test("getInvitation", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            id: "id",
            organization_id: "organization_id",
            inviter: { name: "name" },
            invitee: { email: "email" },
            invitation_url: "invitation_url",
            created_at: "2024-01-15T09:30:00Z",
            expires_at: "2024-01-15T09:30:00Z",
            client_id: "client_id",
            connection_id: "connection_id",
            app_metadata: { key: "value" },
            user_metadata: { key: "value" },
            roles: ["roles"],
            ticket_id: "ticket_id",
        };
        server
            .mockEndpoint()
            .get("/organizations/id/invitations/invitation_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.organizations.getInvitation("id", "invitation_id");
        expect(response).toEqual({
            id: "id",
            organization_id: "organization_id",
            inviter: {
                name: "name",
            },
            invitee: {
                email: "email",
            },
            invitation_url: "invitation_url",
            created_at: "2024-01-15T09:30:00Z",
            expires_at: "2024-01-15T09:30:00Z",
            client_id: "client_id",
            connection_id: "connection_id",
            app_metadata: {
                key: "value",
            },
            user_metadata: {
                key: "value",
            },
            roles: ["roles"],
            ticket_id: "ticket_id",
        });
    });

    test("deleteInvitation", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });

        server
            .mockEndpoint()
            .delete("/organizations/id/invitations/invitation_id")
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.organizations.deleteInvitation("id", "invitation_id");
        expect(response).toEqual(undefined);
    });

    test("createMember", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { members: ["members"] };

        server
            .mockEndpoint()
            .post("/organizations/id/members")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.organizations.createMember("id", {
            members: ["members"],
        });
        expect(response).toEqual(undefined);
    });

    test("deleteMembers", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { members: ["members"] };

        server
            .mockEndpoint()
            .delete("/organizations/id/members")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.organizations.deleteMembers("id", {
            members: ["members"],
        });
        expect(response).toEqual(undefined);
    });

    test("assignMemberRoles", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { roles: ["roles"] };

        server
            .mockEndpoint()
            .post("/organizations/id/members/user_id/roles")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.organizations.assignMemberRoles("id", "user_id", {
            roles: ["roles"],
        });
        expect(response).toEqual(undefined);
    });

    test("deleteMemberRoles", async () => {
        const server = mockServerPool.createServer();
        const client = new ManagementClient({ token: "test", environment: server.baseUrl });
        const rawRequestBody = { roles: ["roles"] };

        server
            .mockEndpoint()
            .delete("/organizations/id/members/user_id/roles")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .build();

        const response = await client.organizations.deleteMemberRoles("id", "user_id", {
            roles: ["roles"],
        });
        expect(response).toEqual(undefined);
    });
});
