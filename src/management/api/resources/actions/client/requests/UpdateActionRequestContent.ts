/**
 * This file was auto-generated by Fern from our API Definition.
 */

import type { ActionTrigger } from "../../../../types/ActionTrigger.js";
import type { ActionVersionDependency } from "../../../../types/ActionVersionDependency.js";
import type { ActionSecretRequest } from "../../../../types/ActionSecretRequest.js";

/**
 * @example
 *     {}
 */
export interface UpdateActionRequestContent {
    /** The name of an action. */
    name?: string;
    /** The list of triggers that this action supports. At this time, an action can only target a single trigger at a time. */
    supported_triggers?: ActionTrigger[];
    /** The source code of the action. */
    code?: string;
    /** The list of third party npm modules, and their versions, that this action depends on. */
    dependencies?: ActionVersionDependency[];
    /** The Node runtime. For example: `node22`, defaults to `node22` */
    runtime?: string;
    /** The list of secrets that are included in an action or a version of an action. */
    secrets?: ActionSecretRequest[];
}
