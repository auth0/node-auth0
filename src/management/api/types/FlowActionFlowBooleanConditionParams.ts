/**
 * This file was auto-generated by Fern from our API Definition.
 */

import type { FlowAction } from "../types/FlowAction.js";

// This is still a circular reference, but it's the best we can do without moving files around.
export interface FlowActionFlowBooleanConditionParams {
    then?: FlowAction[];
    else?: FlowAction[];
}
