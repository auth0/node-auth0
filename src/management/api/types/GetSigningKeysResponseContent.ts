/**
 * This file was auto-generated by Fern from our API Definition.
 */

import type { SigningKeysDate } from "../types/SigningKeysDate.js";

export interface GetSigningKeysResponseContent {
    /** The key id of the signing key */
    kid: string;
    /** The public certificate of the signing key */
    cert: string;
    /** The public certificate of the signing key in pkcs7 format */
    pkcs7?: string;
    /** True if the key is the the current key */
    current?: boolean;
    /** True if the key is the the next key */
    next?: boolean;
    /** True if the key is the the previous key */
    previous?: boolean;
    current_since?: SigningKeysDate;
    current_until?: SigningKeysDate;
    /** The cert fingerprint */
    fingerprint: string;
    /** The cert thumbprint */
    thumbprint: string;
    /** True if the key is revoked */
    revoked?: boolean;
    revoked_at?: SigningKeysDate;
}
